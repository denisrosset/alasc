initialCommands in console := """import net.alasc.finite._; import net.alasc.perms._; import net.alasc.prep._; import net.alasc.syntax.all._; import spire.implicits._"""

addCompilerPlugin("com.github.mpilquist" %% "local-implicits" % "0.3.0")

